{
  "name": "bill-extractor-bot",
  "version": "2.0.0",
  "description": "Telegram bot to extract bill data using Gemini AI and export to Excel",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node -e \"require('dotenv').config(); const {Telegraf}=require('telegraf'); const bot=require('./bot.js'); bot.launch ? bot.launch() : console.log('Use webhook mode');\"",
    "polling": "node polling.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "telegraf": "^4.16.3",
    "form-data": "^4.0.0"
  }
}